# 依赖包，建议在虚拟环境中安装
# 安装命令：
#   pip install -r requirements.txt

# ── 核心依赖 ──
# MediaPipe 手势识别（锁定 0.10.14，新版移除了 solutions API）
mediapipe==0.10.14

# 摄像头采集与图像处理
# ⚠️ 必须用 contrib 版：auth.py 的人脸识别用到 cv2.face 模块（LBPHFaceRecognizer），
# 它只包含在 opencv-contrib-python 中；请勿同时安装 opencv-python（二者会冲突覆盖）
opencv-contrib-python>=4.8.0

# 数组运算（限制 <2.0 防止与 MediaPipe C 扩展冲突）
numpy>=1.24.0,<2.0.0

# 中文文字渲染（Pillow；主程序 PIL 中文叠加时使用）
Pillow>=10.0.0

# GUI 界面框架
PySide6>=6.6.0

# ── 多模态升级依赖 ──
# YOLO-World 物体检测（开放词汇；会随依赖自动安装 torch / torchvision）
ultralytics

# Kimi / Moonshot 大模型（OpenAI 兼容 API，代码使用 v1.x 的 client.chat.completions 接口，需 >=1.0.0）
openai>=1.0.0

# 在线 TTS 语音播报（生成的 mp3 由 pygame mixer 播放）
edge-tts

# TTS 音频播放
pygame

# ASR 语音识别（Faster-Whisper，CPU + INT8 量化，按需懒加载）
faster-whisper

# 实时麦克风录音（main.py 录音用 sounddevice + wave）
sounddevice
