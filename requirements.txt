# 依赖包，建议在虚拟环境中安装
# 安装命令：
#   pip install -r requirements.txt

# MediaPipe 手势识别（锁定 0.10.14，新版移除了 solutions API）
mediapipe==0.10.14

# 摄像头采集与图像处理
opencv-python>=4.8.0

# 数组运算（限制 <2.0 防止与 MediaPipe C 扩展冲突）
numpy>=1.24.0,<2.0.0

# 中文文字渲染（cv2.putText 不支持中文）
Pillow>=10.0.0

# GUI 界面框架
PySide6>=6.6.0