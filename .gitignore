# Python 编译产物
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
*.egg
.eggs/

# 虚拟环境
venv/
.v1/

# IDE
.idea/
.vscode/
*.swp
*.swo

# 系统文件
.DS_Store
Thumbs.db

# 截图 / 日志类的临时文件（自动生成，不入库）
screenshot_*.png
snapshot_*.png
gesture_log_*.csv
history_*.csv
chat_log_*.txt

# 模型权重 / 运行时临时文件（自动生成或过大，不入库）
# ultralytics 下载的 CLIP 权重 weights/clip/
weights/
# YOLO-World 权重（27MB，过大，单独管理）
yolov8s-world.pt
# 运行时 SQLite 数据库（含用户历史记录，本会话内保留，含私密数据）
app_data.db
*.db

# 人脸注册数据（生物特征，禁止入库）
faces/
trainer.yml
auth.db
